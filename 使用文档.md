# CyberDog 控制包简要使用说明

适用包：

```text
control_node_ros2_package_stage4_shift_fix.zip
```

## 1. 解压到工作空间

将压缩包放到：

```text
~/cyberdog_ws/
```

然后执行：

```bash
cd ~/cyberdog_ws

rm -rf src/control_node
unzip control_node_ros2_package_stage4_shift_fix.zip -d src/
```

## 2. 编译

```bash
source /opt/ros2/galactic/setup.bash
source /opt/ros2/cyberdog/setup.bash

cd ~/cyberdog_ws

colcon build   --packages-select control_node   --symlink-install

source install/setup.bash
```

每次重新打开终端后，都需要重新执行：

```bash
source /opt/ros2/galactic/setup.bash
source /opt/ros2/cyberdog/setup.bash
source ~/cyberdog_ws/install/setup.bash
```

## 3. 从第一赛段开始完整运行

```bash
ros2 launch control_node full_competition.launch.py   platform:=real   start_stage:=1
```

运行顺序：

```text
111 起立
→ Stage1
→ Stage2
→ Stage3
→ Stage4
→ Stage5
→ Stage6
```

## 4. 测试指定赛段

测试第二赛段：

```bash
ros2 launch control_node full_competition.launch.py   platform:=real   start_stage:=2
```

测试第三赛段：

```bash
ros2 launch control_node full_competition.launch.py   platform:=real   start_stage:=3
```

测试第四赛段：

```bash
ros2 launch control_node full_competition.launch.py   platform:=real   start_stage:=4
```

测试第五赛段：

```bash
ros2 launch control_node full_competition.launch.py   platform:=real   start_stage:=5
```

测试第六赛段：

```bash
ros2 launch control_node full_competition.launch.py   platform:=real   start_stage:=6
```

例如：

```text
start_stage:=5
```

表示：

```text
111 起立
→ Stage5
→ Stage6
```

不是只运行 Stage5。

如果只想调当前赛段，测试完成后直接：

```text
Ctrl+C
```

停止程序即可。

## 5. 当前真机控制方式

连续运动统一使用：

```text
motion_id = 303
```

启动时程序会先执行：

```text
motion_id = 111
```

即 RecoveryStand。

如果中间执行了跳跃等预设动作，例如：

```text
132 前跳
134 左跳
135 右跳
```

下一次前进、后退、横移或转向时，会重新进入：

```text
303 SERVO_START
→ 等待真机 ACK
→ SERVO_READY
→ SERVO_DATA
```

定时运动会从 `SERVO_READY` 后开始计时。

## 6. 相机话题

RGB：

```text
/mi_desktop_48_b0_2d_7b_00_e2/image_rgb
```

Depth：

```text
/mi_desktop_48_b0_2d_7b_00_e2/camera/depth/image_rect_raw
```

左鱼眼：

```text
/mi_desktop_48_b0_2d_7b_00_e2/image_left
```

右鱼眼：

```text
/mi_desktop_48_b0_2d_7b_00_e2/image_right
```

检查 RGB：

```bash
ros2 topic hz /mi_desktop_48_b0_2d_7b_00_e2/image_rgb
```

检查 Depth：

```bash
ros2 topic hz /mi_desktop_48_b0_2d_7b_00_e2/camera/depth/image_rect_raw
```

## 7. 调试时主要看这些日志

连续运动正常时应看到：

```text
SERVO_START motion_id=303
SERVO_READY motion_id=303
FIRST SERVO_DATA motion_id=303
```

如果出现：

```text
SERVO_START ACK timeout
```

说明当前 303 还没有成功进入控制。

如果 Stage5 出现：

```text
rgb stream stale
P5_SENSOR_FAULT_HOLD
```

优先检查机器狗 RGB 相机。

## 8. 停止程序

直接：

```text
Ctrl+C
```

程序会发送：

```text
SERVO_END
STOP
```

## 9. 最常用命令

编译：

```bash
cd ~/cyberdog_ws

colcon build   --packages-select control_node   --symlink-install

source install/setup.bash
```

完整运行：

```bash
ros2 launch control_node full_competition.launch.py   platform:=real   start_stage:=1
```

测试 Stage4：

```bash
ros2 launch control_node full_competition.launch.py   platform:=real   start_stage:=4
```

测试 Stage5：

```bash
ros2 launch control_node full_competition.launch.py   platform:=real   start_stage:=5
```

测试 Stage6：

```bash
ros2 launch control_node full_competition.launch.py   platform:=real   start_stage:=6
```
